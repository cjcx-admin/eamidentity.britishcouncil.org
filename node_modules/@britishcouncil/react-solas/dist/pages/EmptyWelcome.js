import React from 'react';
import styled from '@emotion/styled';
import {
    SimpleContainer
} from '../containers';
const StyledImgContainer = styled.div `
  margin: 30% auto;
`;
const StyledLogo = styled.img `
  margin: 0 auto;
  width: 100%;
  max-width: 400px;
  filter: grayscale(100%);
  opacity: 0.1;
`;
/**
 * It is a simple empty page with BC logo inside. It can be used as a temporary placeholder for further content.
 */
export function EmptyWelcome() {
    return (React.createElement(SimpleContainer, {
            centered: true,
            size: "medium"
        },
        React.createElement(StyledImgContainer, null,
            React.createElement(StyledLogo, {
                src: "https://bccdn.azureedge.net/dev/bc-logo-black.svg",
                alt: "British Council Logo"
            }))));
}