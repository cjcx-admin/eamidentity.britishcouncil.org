import styled from '@emotion/styled';
const getSize = (size) => {
    switch (size) {
        case 'mini':
            return 10;
        case 'tiny':
            return 12;
        case 'small':
            return 16;
        case 'medium':
            return 18;
        case 'large':
            return 24;
        case 'big':
            return 32;
        case 'huge':
            return 64;
        case 'massive':
            return 128;
        default:
            return 18;
    }
};
export const Icon = styled.div `
  display: inline-block;
  margin: ${({ fitted }) => fitted ? 0 : '0 5px'};
  width: ${({ size }) => getSize(size)}px;
  opacity: ${({ disabled }) => (disabled ? 0.5 : 1)};
  animation: ${({ loading }) => (loading ? 'spin-cw 2s infinite linear' : 'none')};
  height: ${({ loading, size }) => loading ? `
$ {
    getSize(size)
}
px ` : 'auto'};
  cursor: ${({ link }) => (link ? 'pointer' : 'auto')};
  fill: ${({ color }) => (color ?? 'black')};

  @keyframes spin-cw {
    0% {
      transform: rotate(0);
    }
    100% {
      transform: rotate(359deg);
    }
  }
`;