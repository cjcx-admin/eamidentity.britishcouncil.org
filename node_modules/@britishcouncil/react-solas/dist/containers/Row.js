import React from 'react';
import cn from 'classnames';
export const Row = ({
    children,
    ...props
}) => {
    return (React.createElement("div", { ...props,
        className: cn('row', props.className)
    }, children));
};