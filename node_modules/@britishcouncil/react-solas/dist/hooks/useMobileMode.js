import {
    useWindowSize
} from '@britishcouncil/react-common';
const mobileScreenWidthLimit = 850;
export function useMobileMode() {
    const {
        width
    } = useWindowSize();
    if (!width) {
        return false;
    }
    return width < mobileScreenWidthLimit;
}