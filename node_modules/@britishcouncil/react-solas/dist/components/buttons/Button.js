import React from 'react';
import cn from 'classnames';
export const Button = ({
    size,
    loading,
    loaded,
    square,
    block,
    intent = 'default',
    children,
    ...props
}) => {
    return (React.createElement("button", { ...props,
            className: cn('btn', `btn-${intent}`, props.className, {
                'btn-is-loading': loading,
                'btn-has-loaded': loaded,
                'btn-square': square,
                'btn-block': block,
                [`btn-${size}`]: size,
            })
        },
        loading || loaded ? React.createElement("span", {
            className: "bc-loader"
        }) : null,
        children));
};