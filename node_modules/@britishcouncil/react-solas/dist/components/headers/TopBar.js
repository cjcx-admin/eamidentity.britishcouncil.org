import React from 'react';
import styled from '@emotion/styled';
import * as S from './SimpleHeader.styles';
const StyledHeaderInfoAndSecondaryNavContainer = styled.div `
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  column-gap: 40px;
  row-gap: 25px;
  width: 100%;
`;
const StyledTopBarContainer = styled.div `
  ${props => props.flex ? `display: flex;` : null}

  width: 100%;
`;
StyledTopBarContainer.defaultProps = {
    flex: false
};
export function TopBar(props) {
    const isInfoBoxVisible = !!props.infoBox;
    const isSecondaryNavVisible = !!props.secondaryNav;
    return (React.createElement(StyledTopBarContainer, {
            flex: !isInfoBoxVisible && isSecondaryNavVisible
        }, isInfoBoxVisible && isSecondaryNavVisible ?
        React.createElement(StyledHeaderInfoAndSecondaryNavContainer, null,
            React.createElement("div", null, props.infoBox),
            React.createElement(S.SecondaryNavContainer, null, props.secondaryNav)) :
        React.createElement(React.Fragment, null, isInfoBoxVisible ?
            props.infoBox :
            React.createElement(S.SecondaryNavContainer, null, props.secondaryNav))));
}