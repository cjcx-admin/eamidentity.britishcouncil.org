import React, {
    useContext
} from 'react';
import {
    NavLink
} from 'react-router-dom';
import {
    Icon
} from '../../icons';
import {
    MobileMenuContext
} from '.';
/**
 * Basic link to be used in Header navs
 */
export function Link(props) {
    const {
        closeMenu
    } = useContext(MobileMenuContext);
    const iconPosition = props.iconPosition ? ? 'left';
    return (React.createElement("li", null,
        React.createElement(NavLink, {
                to: props.to,
                onClick: closeMenu
            },
            props.icon && iconPosition === 'left' && React.createElement(Icon, {
                style: props.iconStyle
            }, props.icon),
            props.name,
            props.icon && iconPosition === 'right' && React.createElement(Icon, {
                style: props.iconStyle
            }, props.icon))));
}