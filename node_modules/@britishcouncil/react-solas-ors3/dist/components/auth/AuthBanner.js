import React from 'react';
import {
    Alert
} from '@britishcouncil/react-solas';
import styled from '@emotion/styled';
import {
    FiCheck
} from 'react-icons/fi';
import {
    MainContainer
} from '../layout';
export const AuthBanner = ({
    children
}) => {
    return (React.createElement(MainContainer, null,
        React.createElement(OkAlert, {
                type: "success",
                "aria-live": "polite"
            },
            React.createElement(FiCheck, {
                "aria-hidden": "true"
            }),
            children)));
};
const OkAlert = styled(Alert)
`
  border-radius: 3px;

  > svg {
    width: 2em;
    height: 2em;
    margin: -4px 15px -12px 0;

    [dir='rtl'] & {
      margin: -4px 0 -12px 15px;
      transform: scaleX(1) !important;
    }
  }
`;