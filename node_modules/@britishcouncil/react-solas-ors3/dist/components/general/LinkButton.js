import React from 'react';
import cn from 'classnames';
export const LinkButton = ({ ...props
}) => {
    const className = cn('btn-link', 'link', props.className);
    return (React.createElement("button", {
        type: "button",
        ...props,
        className: className
    }, props.children));
};