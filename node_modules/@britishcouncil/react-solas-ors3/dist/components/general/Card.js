import React from 'react';
import styled from '@emotion/styled';
export const Card = ({
    children,
    ...props
}) => {
    return React.createElement(StyledCard, { ...props
    }, children);
};
const StyledCard = styled.div `
  border-radius: 12px;
  box-shadow: 0px 4px 4px 0px rgba(0, 0, 0, 0.25);
  padding: 24px;
  background: #fff;

  & + & {
    margin-top: 32px;
  }
`;