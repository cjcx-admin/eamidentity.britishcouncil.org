import React from 'react';
import styled from '@emotion/styled';
export const Divider = ({
    margin = '32px 0'
}) => React.createElement(DividerBase, {
    margin: margin
});
const DividerBase = styled.hr `
  && {
    border-top: 4px dotted #8f9fad;
    margin: ${({ margin }) => margin};
  }
`;