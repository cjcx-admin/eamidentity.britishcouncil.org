import React from 'react';
import cn from 'classnames';
const SuiPanelBody = ({
    children,
    className,
    ...props
}) => (React.createElement("div", {
    className: `panel-body ${className}`,
    ...props
}, children));
const SuiPanelTable = ({
    children,
    className,
    ...props
}) => (React.createElement("table", {
    className: `table ${className}`,
    ...props
}, children));
const SuiPanelHeader = ({
    children,
    className,
    ...props
}) => (React.createElement("div", {
    className: `panel-heading ${className}`,
    "data-testid": "modal",
    ...props
}, children));
export const SuiPanel = ({
    hasBorders = false,
    fullWidth = false,
    hasIcons = false,
    hasIcon = false,
    promo = false,
    hollow = false,
    intent = 'default',
    title = '',
    children,
    className,
    ...props
}) => (React.createElement("aside", {
        className: cn('panel', 'panel-' + intent, {
            'panel-has-borders': hasBorders,
            'bc-full-width-xs': fullWidth,
            'panel-has-icons': hasIcons,
            'panel-has-icon': hasIcon,
            'panel-promo': promo,
            'panel-hollow': hollow,
        }, className),
        ...props
    }, !!title && React.createElement("div", {
        className: "panel-heading"
    }, title),
    children));
SuiPanel.Body = SuiPanelBody;
SuiPanel.Table = SuiPanelTable;
SuiPanel.Header = SuiPanelHeader;