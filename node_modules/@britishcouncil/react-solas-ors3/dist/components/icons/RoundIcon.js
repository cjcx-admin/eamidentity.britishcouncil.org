import React from 'react';
import styled from '@emotion/styled';
const RoundBackground = styled.div `
  width: 48px;
  height: 48px;
  border-radius: 50px;
  background-color: #ebeff0;
  display: inline-flex;
  justify-content: center;
  align-items: center;
`;
export const RoundIcon = ({
    children,
    ...props
}) => {
    return React.createElement(RoundBackground, { ...props
    }, children);
};