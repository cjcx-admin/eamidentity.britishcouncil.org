import React from 'react';
import styled from '@emotion/styled';
import {
    LinkButton
} from '../..';
const StyledHeader = styled.div `
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  @media (max-width: 600px) {
    > button {
      display: block;
      margin-left: -7px;
    }
  }
`;
export const SectionHeader = ({
    children,
    actionName,
    actionCallback,
    ...props
}) => {
    const onAction = (e) => {
        e.preventDefault();
        if (actionCallback) {
            actionCallback();
        }
    };
    return (React.createElement(StyledHeader, { ...props
        },
        React.createElement("h2", null,
            React.createElement("b", null, children)),
        actionName && actionCallback && React.createElement(LinkButton, {
            onClick: onAction
        }, actionName)));
};