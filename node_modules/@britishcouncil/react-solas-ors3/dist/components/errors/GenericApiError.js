import React from 'react';
import {
    getErrorCode
} from '@britishcouncil/react-common';
import {
    BcAlert
} from '../general/BcAlert';
const i18nDefault = {
    errorTitle: 'Oooops!',
    defaultErrorMsg: 'It looks like something went wrong. Please try again in a few minutes.',
};
export const GenericApiError = ({
    apiError,
    children,
    detailedMsg,
    i18n = i18nDefault,
    ...props
}) => {
    const errType = getErrorCode(apiError);
    if (!errType)
        return null;
    return (React.createElement(BcAlert, { ...props,
            type: "danger",
            title: i18n.errorTitle
        },
        detailedMsg ? ? i18n.defaultErrorMsg,
        children));
};