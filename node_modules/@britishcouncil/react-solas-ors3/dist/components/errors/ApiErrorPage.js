import React from 'react';
import {
    MainContainer
} from '../layout';
import {
    BcAlert
} from '../general';
export const ApiErrorPage = ({
    children,
    title = 'Oooops!',
    errorMessage = 'It looks like something went wrong. Please try again in a few minutes.',
}) => {
    return (React.createElement(MainContainer, null,
        React.createElement(BcAlert, {
            type: "danger",
            title: title
        }, errorMessage),
        children));
};