import React from 'react';
import cn from 'classnames';
import styled from '@emotion/styled';
import {
    IcoTick,
    IcoCross
} from '@britishcouncil/react-solas';
export const CheckItem = ({
    checked,
    text
}) => {
    return (React.createElement(StyledLi, null,
        React.createElement("span", {
                className: cn('check-item', {
                    'text-success': checked
                })
            },
            checked ? React.createElement(IcoTick, {
                "aria-hidden": "true"
            }) : React.createElement(IcoCross, {
                "aria-hidden": "true"
            }),
            text)));
};
const StyledLi = styled.li `
  svg {
    fill: #555;
    margin: 0 6px -2px 0;
    width: 1em;
    height: 1em;
  }

  &.text-success svg {
    fill: #3c763d;
  }
`;