import React from 'react';
import cn from 'classnames';
export const ReadonlyField = ({
    label,
    horizontal = true,
    childrenDir = undefined,
    children,
    ...props
}) => (React.createElement("dl", { ...props,
        className: cn({
            'dl-horizontal': horizontal
        }, props.className)
    },
    React.createElement("dt", null,
        label,
        ":"),
    React.createElement("dd", {
        dir: childrenDir
    }, children)));