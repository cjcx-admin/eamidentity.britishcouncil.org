import {
    isComparableNumber
} from '../utils';
const isGreater = (fieldValue, compareTo) => !isComparableNumber(fieldValue) || fieldValue > compareTo;
const validateGreater = (fieldValue, {
    property,
    value
}, formValues) => isGreater(fieldValue, !!property ? Number(formValues[property]) : Number(value));
export default validateGreater;