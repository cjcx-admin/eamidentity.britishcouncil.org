import {
    isComparableNumber
} from '../utils';
const isEqual = (fieldValue, compareTo) => !isComparableNumber(fieldValue) || Number(fieldValue) === compareTo;
const validateEqual = (fieldValue, {
    property,
    value
}, formValues) => isEqual(fieldValue, !!property ? Number(formValues[property]) : Number(value));
export default validateEqual;