import validateEqual from './validateEqual';
const validateNotEqual = (fieldValue, {
    property,
    value
}, formValues) => !validateEqual(fieldValue, {
    property,
    value
}, formValues);
export default validateNotEqual;