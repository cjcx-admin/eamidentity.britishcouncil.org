import {
    isComparableNumber
} from '../utils';
const isLess = (fieldValue, compareTo) => !isComparableNumber(fieldValue) || fieldValue < compareTo;
const validateLess = (fieldValue, {
    property,
    value
}, formValues) => isLess(fieldValue, !!property ? Number(formValues[property]) : Number(value));
export default validateLess;