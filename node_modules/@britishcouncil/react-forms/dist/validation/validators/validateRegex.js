const getRegexObject = (pattern, ignoreCase) => new RegExp(pattern, ignoreCase ? 'i' : undefined);
const validateRegex = (fieldValue, {
    ignoreCase,
    regex
}) => !regex || !fieldValue || getRegexObject(regex, ignoreCase).test(fieldValue);
export default validateRegex;