import validateEqual from './validateEqual';
import validateLess from './validateLess';
const validateLessOrEqual = (fieldValue, {
        property,
        value
    }, formValues) => validateLess(fieldValue, {
        property,
        value
    }, formValues) ||
    validateEqual(fieldValue, {
        property,
        value
    }, formValues);
export default validateLessOrEqual;