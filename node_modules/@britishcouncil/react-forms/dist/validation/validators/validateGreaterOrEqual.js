import validateEqual from './validateEqual';
import validateGreater from './validateGreater';
const validateGreaterOrEqual = (fieldValue, {
        property,
        value
    }, formValues) => validateGreater(fieldValue, {
        property,
        value
    }, formValues) ||
    validateEqual(fieldValue, {
        property,
        value
    }, formValues);
export default validateGreaterOrEqual;