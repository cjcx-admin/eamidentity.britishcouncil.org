const validateLength = (fieldValue = '', {
    min,
    max
}) => {
    const length = fieldValue ? .length ? ? 0;
    return (max === undefined || length <= max) && (min === undefined || length >= min);
};
export default validateLength;