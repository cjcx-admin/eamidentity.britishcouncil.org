import React from 'react';
import {
    withoutAdditionalProps
} from './propHelpers';
export const NumberInput = (props) => {
    const {
        inputRef,
        onChange,
        onBlur,
        value,
        ...inputProps
    } = props;
    return (React.createElement("input", { ...withoutAdditionalProps(inputProps),
        type: "number",
        ref: inputRef,
        onBlur: () => props.onBlur(),
        onChange: e => {
            const value = e.target.value;
            if (typeof value !== 'number' && !value)
                return props.onChange((props.required ? 0 : undefined));
            props.onChange(Number(value));
        },
        value: value ? ? ''
    }));
};