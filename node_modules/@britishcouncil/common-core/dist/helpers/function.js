export const identity = (obj) => obj;
export const not = (func) => ((...args) => !func(...args));
export function noop(..._) {
    /* noop */
}
/** Use it only for verifying types in unit tests */
export function assertEqual(shouldBeTrue) {}