import ReactGA from 'react-ga';
import TagManager from 'react-gtm-module';
/**
 * Initializes Google Analytics & Google Tag Manager
 * @param gtmCode GTM id/code, must be something like GTM-000000.
 */
export function initialize(gtmCode, gtmAuth, previewEnv) {
    if (!gtmCode) {
        throw new Error('There is no valid GTM code. Please provide it by `setupGtmCode` method.');
    }
    TagManager.initialize({
        gtmId: gtmCode,
        auth: gtmAuth,
        preview: previewEnv,
    });
    ReactGA.initialize(gtmCode);
    ReactGA.pageview(window.location.pathname);
}
export function updateDataLayer(dataLayer) {
    TagManager.dataLayer({
        dataLayer: dataLayer,
        dataLayerName: 'dataLayer',
    });
}
//# sourceMappingURL=googleTagManager.js.map