import React from 'react';
import {
    ThemeProvider,
    useTheme,
    Global
} from '@emotion/react';
import {
    globalExtroStyles
} from './styles';
import {
    theme
} from './theme';
export const ExtroProvider = ({
    children
}) => {
    return (React.createElement(ThemeProvider, {
            theme: theme
        },
        React.createElement(Global, {
            styles: globalExtroStyles
        }),
        children));
};
export const useExtroTheme = () => useTheme();