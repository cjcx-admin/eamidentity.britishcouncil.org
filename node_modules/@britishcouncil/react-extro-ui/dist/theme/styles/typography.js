import {
    css
} from '@emotion/react';
import {
    theme
} from '../theme';
const baseFontUrl = 'https://bccdn.britishcouncil.org/dev/solas/0.3.3/fonts';
export const typography = css `
  @font-face {
    font-family: BCSans;
    src:
      url('${baseFontUrl}/BritishCouncilSansW08-Lt.woff2') format('woff2'),
      url('${baseFontUrl}/BritishCouncilSansW08-Lt.woff') format('woff');
    font-weight: 100;
  }

  @font-face {
    font-family: BCSans;
    src:
      url('${baseFontUrl}/BritishCouncilSansW08-Rg.woff2') format('woff2'),
      url('${baseFontUrl}/BritishCouncilSansW08-Rg.woff') format('woff');
    font-weight: 300;
    font-style: normal;
  }

  @font-face {
    font-family: BCSans;
    src:
      url('${baseFontUrl}/BritishCouncilSansW08-Bd.woff2') format('woff2'),
      url('${baseFontUrl}/BritishCouncilSansW08-Bd.woff') format('woff');
    font-weight: 600;
  }

  html {
    font-family: Arial, Helvetica, sans-serif;
    font-size: ${theme.fontSize.default};
    line-height: 150%;
    font-weight: 400;
  }

  h1 {
    font-family: BCSans, Arial, Helvetica, sans-serif;
    font-weight: 300;
    line-height: 130%;
    color: ${theme.colors.indigo};

    ${theme.mediaQueries.xs} {
      font-size: ${theme.fontSize.mobile.h1};
    }

    ${theme.mediaQueries.md} {
      font-size: ${theme.fontSize.desktop.h1};
    }
  }

  h2 {
    font-family: BCSans, Arial, Helvetica, sans-serif;
    font-weight: 700;
    line-height: 130%;
    color: ${theme.colors.indigo};

    ${theme.mediaQueries.xs} {
      font-size: ${theme.fontSize.mobile.h2};
    }

    ${theme.mediaQueries.md} {
      font-size: ${theme.fontSize.desktop.h2};
    }
  }

  h3 {
    font-weight: 700;
    line-height: 130%;
    color: ${theme.colors.indigo};

    ${theme.mediaQueries.xs} {
      font-size: ${theme.fontSize.mobile.h3};
    }

    ${theme.mediaQueries.md} {
      font-size: ${theme.fontSize.desktop.h3};
    }
  }

  h4 {
    font-weight: 700;
    line-height: 130%;
    color: ${theme.colors.indigo};

    ${theme.mediaQueries.xs} {
      font-size: ${theme.fontSize.mobile.h4};
    }

    ${theme.mediaQueries.md} {
      font-size: ${theme.fontSize.desktop.h4};
    }
  }

  h5 {
    font-weight: 700;
    line-height: 130%;
    color: ${theme.colors.indigo};

    ${theme.mediaQueries.xs} {
      font-size: ${theme.fontSize.mobile.h5};
    }

    ${theme.mediaQueries.md} {
      font-size: ${theme.fontSize.desktop.h5};
    }
  }

  h6 {
    font-weight: 700;
    line-height: 130%;
    color: ${theme.colors.indigo};

    ${theme.mediaQueries.xs} {
      font-size: ${theme.fontSize.mobile.h6};
    }

    ${theme.mediaQueries.md} {
      font-size: ${theme.fontSize.desktop.h6};
    }
  }

  a {
    font-weight: 700;
    line-height: 150%;
    text-decoration: underline;
    color: ${theme.colors.darkBlue};

    ${theme.mediaQueries.xs} {
      font-size: ${theme.fontSize.mobile.body};
    }

    ${theme.mediaQueries.md} {
      font-size: ${theme.fontSize.desktop.body};
    }
  }

  small {
    line-height: 150%;

    ${theme.mediaQueries.xs} {
      font-size: ${theme.fontSize.mobile.small};
    }

    ${theme.mediaQueries.md} {
      font-size: ${theme.fontSize.desktop.small};
    }
  }

  strong {
    font-weight: 700;
  }
`;