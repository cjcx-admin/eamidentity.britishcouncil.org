import {
    css
} from '@emotion/react';
import {
    reset
} from './reset';
import {
    typography
} from './typography';
import {
    layout
} from './layout';
export const globalExtroStyles = css `
  ${reset};
  ${typography};
  ${layout}
`;