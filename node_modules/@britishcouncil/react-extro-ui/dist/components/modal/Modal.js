import React from 'react';
import * as S from './Modal.style';
export const Modal = ({
    children,
    ModalActions,
    title,
    onClose,
    ...restProps
}) => {
    return (React.createElement(S.ModalContainer, {
            overlayElement: (props, contentElement) => React.createElement(S.ModalOverlay, { ...props
            }, contentElement),
            ariaHideApp: false,
            onRequestClose: onClose,
            ...restProps
        },
        title && React.createElement(S.ModalHeader, null, title),
        React.createElement(S.ModalBody, null,
            children,
            ModalActions && React.createElement(S.ModalFooter, null, ModalActions))));
};