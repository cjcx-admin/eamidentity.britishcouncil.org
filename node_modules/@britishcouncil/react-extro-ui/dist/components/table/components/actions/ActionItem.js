import React from 'react';
import styled from '@emotion/styled';
export const ActionItem = ({
    Icon,
    text = '',
    danger = false,
    ...divProps
}) => {
    return (React.createElement(Root, {
            danger: danger,
            ...divProps,
            tabIndex: 0
        },
        Icon,
        React.createElement("span", null, text)));
};
const Root = styled.div `
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 20px;
  font-weight: 700;
  font-size: 14px;
  line-height: 16px;
  color: ${({ danger, theme }) => (danger ? theme.colors.red : theme.colors.darkBlue)};
  cursor: pointer;
  span {
    max-width: 200px;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  svg {
    min-width: 16px;
  }
`;