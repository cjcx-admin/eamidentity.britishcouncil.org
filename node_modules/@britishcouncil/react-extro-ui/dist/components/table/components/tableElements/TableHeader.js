import React from 'react';
import {
    flexRender
} from '@tanstack/react-table';
export const TableHeader = ({
    table
}) => {
    return (React.createElement(React.Fragment, null, table.getHeaderGroups().map((headerGroup) => (React.createElement("tr", {
        key: headerGroup.id
    }, headerGroup.headers.map((header) => (React.createElement("th", {
        key: header.id,
        style: {
            width: header.getSize() === 0 ? undefined : header.getSize()
        }
    }, header.isPlaceholder ? null : flexRender(header.column.columnDef.header, header.getContext())))))))));
};