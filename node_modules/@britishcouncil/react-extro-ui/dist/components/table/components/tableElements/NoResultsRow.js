import React from 'react';
import styled from '@emotion/styled';
export const NoResultsRow = () => React.createElement(StyledEmpty, null, "No results found");
const StyledEmpty = styled.div `
  text-align: center;
`;