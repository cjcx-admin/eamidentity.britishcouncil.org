import React from 'react';
import styled from '@emotion/styled';
export const InfoRows = ({
    infoData,
    rowData,
    colSpan
}) => {
    const extraColumns = infoData ? .filter((el) => el.show);
    return (React.createElement(React.Fragment, null, extraColumns ?
        extraColumns.map(({
            Content,
            backgroundColor
        }, index) => (React.createElement(StyledRow, {
                key: index,
                backgroundColor: backgroundColor
            },
            React.createElement("td", null),
            React.createElement("td", {
                    colSpan: colSpan - 1
                },
                React.createElement(Content, {
                    data: rowData
                }))))) :
        null));
};
const StyledRow = styled.tr `
  &&& {
    height: auto;
    background-color: ${({ backgroundColor }) => backgroundColor ?? '#FFBAB880'};
  }
`;