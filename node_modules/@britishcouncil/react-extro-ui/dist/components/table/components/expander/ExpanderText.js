import styled from '@emotion/styled';
import React from 'react';
import {
    FiChevronDown,
    FiChevronUp
} from 'react-icons/fi';
import {
    Link
} from '../Link';
export const ExpanderText = ({
    labels,
    expanded,
    disabled
}) => (React.createElement(StyledText, null,
    React.createElement(Link, {
        disabled: disabled
    }, disabled ? labels ? .disabled : labels ? .enabled),
    disabled ? null : expanded ? React.createElement(FiChevronUp, null) : React.createElement(FiChevronDown, null)));
const StyledText = styled.div `
  display: flex;
  align-items: center;
  gap: 12px;
  svg {
    color: ${({ theme: { colors } }) => colors.darkBlue};
    stroke-width: 4px;
  }
`;