import styled from '@emotion/styled';
import React from 'react';
export const Link = ({
    children,
    disabled,
    ...rest
}) => {
    return (React.createElement(StyledLink, {
        disabled: disabled,
        ...rest,
        tabIndex: disabled ? -1 : 0
    }, children));
};
const StyledLink = styled.a `
  color: ${({ disabled, theme }) => (disabled ? 'auto' : theme.colors.darkBlue)};
  font-weight: ${({ disabled }) => (disabled ? '400' : '700')};
  font-size: 14px;
  cursor: ${({ disabled }) => (disabled ? 'auto' : 'pointer')};
  &:hover {
    text-decoration: ${({ disabled }) => (disabled ? 'auto' : 'underline')};
  }
`;