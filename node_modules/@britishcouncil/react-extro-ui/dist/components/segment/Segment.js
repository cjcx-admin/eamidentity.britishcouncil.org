import React from 'react';
import * as S from './Segment.styles';
export const Segment = ({
    elevation = 1,
    color,
    selectable,
    selected,
    children,
    className,
    ...rest
}) => {
    return (React.createElement(S.Root, {
        elevation: elevation,
        color: color || 'white',
        selectable: selectable,
        selected: selected,
        ...rest,
        className: `ex-segment ${className}`
    }, children));
};