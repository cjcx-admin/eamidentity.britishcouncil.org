import styled from '@emotion/styled';
import {
    colorWithOpacity
} from '../../theme';
export const StyledChip = styled.div `
  display: inline-flex;
  justify-content: center;
  align-items: center;
  padding: ${({ theme: { spacing } }) => `
$ {
    spacing.sm
}
$ {
    spacing.md
}
`};
  height: 32px;
  border-radius: 50px;
  font-size: ${({ theme }) => theme.fontSize.default};
  color: ${({ disabled, textColor, theme }) => (disabled ? theme.colors.darkGrey : textColor)};
  background-color: ${({ disabled, background }) => disabled ? (background === 'transparent' ? background : colorWithOpacity('lightGrey', 0.5)) : background};
`;
export const Dot = styled.div `
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background-color: ${({ color, disabled, theme }) => (disabled ? theme.colors.darkGrey : color)};
  margin-inline-end: ${({ theme }) => theme.spacing.sm};
`;