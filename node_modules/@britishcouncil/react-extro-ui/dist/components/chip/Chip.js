import React from 'react';
import {
    Dot,
    StyledChip
} from './Chip.styles';
export const Chip = ({
    children,
    disabled,
    textColor = '#000',
    dotColor,
    background = 'transparent',
    ...divProps
}) => {
    return (React.createElement(StyledChip, { ...divProps,
            disabled: disabled,
            textColor: textColor,
            background: background
        },
        dotColor && React.createElement(Dot, {
            "data-testid": "chip-dot",
            color: dotColor,
            disabled: disabled
        }),
        children));
};