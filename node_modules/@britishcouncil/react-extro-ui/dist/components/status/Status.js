import React from 'react';
import * as S from './Status.styles';
export const Status = ({
    content,
    color = 'grey',
    variant = 'transparent'
}) => {
    return (React.createElement(S.Root, {
        color: color,
        variant: variant
    }, content));
};