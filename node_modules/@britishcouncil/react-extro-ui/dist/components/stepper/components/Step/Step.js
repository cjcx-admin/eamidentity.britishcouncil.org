import React from 'react';
import * as S from './Step.style';
export const Step = ({
    isActive,
    number,
    testId
}) => (React.createElement(S.StepContainer, {
        "data-testid": testId
    },
    React.createElement(S.Circle, {
        isActive: isActive
    }, number)));