import styled from '@emotion/styled';
export const StepContainer = styled.div `
  display: flex;
  align-items: center;
  justify-content: center;
`;
export const Circle = styled.div `
  height: 32px;
  width: 32px;
  border-radius: 50%;
  background: ${({ isActive, theme }) => (isActive ? theme.colors.indigo : 'transparent')};
  color: ${({ isActive, theme }) => (isActive ? 'white' : theme.colors.indigo)};
  border: 2px solid ${({ theme }) => theme.colors.indigo};
  text-align: center;

  display: flex;
  align-items: center;
  justify-content: center;
`;