import styled from '@emotion/styled';
export const MainWrapper = styled.div `
  width: 100%;
`;
export const InnerWrapper = styled('div', {
    shouldForwardProp: (prop) => !['customWidth', 'sticky', 'offsetHeight'].includes(prop),
})
`
  ${({ sticky, offsetHeight }) => sticky &&
    `
position: fixed;
$ {
    offsetHeight && `top: ${offsetHeight};`
}
z - index: 999;
`};
  ${({ customWidth, sticky }) => sticky &&
    customWidth &&
    `
width: $ {
    customWidth
};
`};
`;