import React from 'react';
import * as S from './Card.styles';
const RenderContent = ({
    content,
    tag
}) => {
    if (typeof content === 'string') {
        return React.createElement(tag, null, content);
    }
    return React.createElement(React.Fragment, null, content);
};
export const Card = ({
    title,
    subtitle,
    content,
    buttons,
    ...rest
}) => {
    return (React.createElement(S.Root, { ...rest
        },
        React.createElement(S.ContentWrapper, null,
            title && (React.createElement(S.Title, null,
                React.createElement(RenderContent, {
                    content: title,
                    tag: "h5"
                }))),
            subtitle && (React.createElement(S.SubTitle, null,
                React.createElement(RenderContent, {
                    content: subtitle,
                    tag: "h6"
                }))),
            content && (React.createElement(S.Content, null,
                React.createElement(RenderContent, {
                    content: content,
                    tag: "small"
                }))),
            buttons && React.createElement(S.Actions, null, buttons))));
};