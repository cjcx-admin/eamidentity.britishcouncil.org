import {
    usePermissions
} from '.';
import {
    hasRequiredPermissions
} from './hasRequiredPermissions';
export function useAllPermissions(...requiredPermissions) {
    const {
        permissions
    } = usePermissions();
    return hasRequiredPermissions(permissions, requiredPermissions);
}