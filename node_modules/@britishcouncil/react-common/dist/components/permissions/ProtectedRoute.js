import React from 'react';
import {
    Route
} from 'react-router';
import {
    Protected
} from './Protected';
export const ProtectedRoute = (props) => {
    return (React.createElement(Protected, { ...props
        },
        React.createElement(Route, { ...props
        })));
};