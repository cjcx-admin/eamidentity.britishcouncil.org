import React from 'react';
import {
    useOutsideClickRef
} from '../hooks';
const OutsideClickNotifier = ({
    onOutsideClick,
    ...restOfProps
}) => {
    const outsideRef = useOutsideClickRef(onOutsideClick);
    return React.createElement("div", {
        ref: outsideRef,
        ...restOfProps
    });
};
export default OutsideClickNotifier;