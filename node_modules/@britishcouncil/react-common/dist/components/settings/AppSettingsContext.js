import React, {
    useContext
} from 'react';
export var AppSettingsStatus;
(function(AppSettingsStatus) {
    AppSettingsStatus["loading"] = "loading";
    AppSettingsStatus["loaded"] = "loaded";
    AppSettingsStatus["error"] = "error";
})(AppSettingsStatus || (AppSettingsStatus = {}));
/**
 * Method to define AppSettingsContext and useAppSettings hook
 */
export function createAppSettingsContext() {
    const AppSettingsContext = React.createContext({});
    const useAppSettings = () => useContext(AppSettingsContext);
    return {
        useAppSettings,
        AppSettingsContext
    };
}