import {
    useEffect
} from 'react';
import {
    usePrevious
} from './usePrevious';
export function useValueWatch(value, changeHandler) {
    const oldValue = usePrevious(value);
    useEffect(() => {
        changeHandler(oldValue, value);
    }, [value]); // eslint-disable-line react-hooks/exhaustive-deps
}