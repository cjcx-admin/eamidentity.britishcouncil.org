import {
    useState,
    useCallback
} from 'react';
import {
    useResizeObserverCallback
} from './useResizeObserverCallback';
export function useResizeObserver() {
    const [rect, setRect] = useState(null);
    const roRef = useResizeObserverCallback(useCallback(newRect => {
        setRect(newRect);
    }, []));
    return [rect, roRef];
}