import {
    useEffect,
    useRef
} from 'react';
import isEqual from 'react-fast-compare';
export function useDeepCompareMemo(value) {
    const ref = useRef();
    if (!isEqual(value, ref.current)) {
        ref.current = value;
    }
    return ref.current;
}
export const useDeepCompareEffect = (effect, deps) => useEffect(effect, useDeepCompareMemo(deps));