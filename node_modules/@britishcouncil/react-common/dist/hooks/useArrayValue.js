import {
    useMemo
} from 'react';
export function getArrayValue(value = []) {
    const emptyArray = [];
    return emptyArray.concat(value);
}
export function useArrayValue(value = []) {
    return useMemo(() => getArrayValue(value), [value]);
}