import {
    useCallback,
    useState
} from 'react';
export function useBoolean(initialState = false) {
    const [state, setState] = useState(initialState);
    const toggle = useCallback(value => (value === null || value === undefined ? setState(previous => !previous) : setState(value)), []);
    return [state, toggle];
}