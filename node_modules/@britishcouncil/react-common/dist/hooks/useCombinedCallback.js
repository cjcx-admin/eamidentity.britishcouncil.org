/* eslint-disable react-hooks/exhaustive-deps */
import {
    useCallback
} from 'react';
export function useCombinedCallback(...functions) {
    return useCallback((...args) => {
        for (const func of functions) {
            if (func) {
                func(...args);
            }
        }
    }, functions);
}